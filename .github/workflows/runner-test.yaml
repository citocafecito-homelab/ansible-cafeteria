name: Test ARC Runner
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: [self-hosted, linux, x64, citocafecito-runner]
    steps:
      - run: echo "✅ ARC runner funcionando en namespace apps-github"
